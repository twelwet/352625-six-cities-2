import React from "react";

const Loading = () => {
  return (
    <div style={{
      position: `absolute`,
      top: `50%`,
      left: `50%`
    }}
    >Loading...</div>
  );
};

export default Loading;
